syntax para = ctx => {
    let forExpression = ctx.next().value;
    let forContext = ctx.next().value;
    console.log(forExpression);
    console.log("\n");
    console.log(forContext);
    let result = #`for ${ifExpression} ${ifContext}`;
  return result;
};